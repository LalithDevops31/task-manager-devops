version: '3.8'

services:

  backend:
    build: ./backend
    container_name: my-backend
    ports:
      - "3001:3001"

  frontend:
    build: ./frontend
    container_name: my-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend